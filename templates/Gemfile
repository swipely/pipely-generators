if !File.exists?('packed_flag')
  source 'https://rubygems.org'
end

gem 'fog'
gem "rake", '~> 10.1.0'
gem "streamingly", '~> 0.0.5'
gem "pipely", '~> 0.6.2'

group :development, :test do
  gem "rspec"
end
